# AI 맞춤형 관광 추천 서비스

공주정보고 경영정보과 실습 프로젝트. 최종적으로 고등학생이 읽고 수정할 코드다.

## 절대 규칙
- Python 3.11 이상만 사용(현재 환경 3.14). TypeScript/React 금지.
- 웹 UI는 Streamlit만. FastAPI/Flask 금지.
- 추천은 pandas + scikit-learn 표준 API만. 커스텀 프레임워크·딥러닝(torch/tensorflow) 도입 금지.
- API 키는 반드시 `os.getenv()`로 읽는다. 코드에 하드코딩 금지.
- `data/raw/` 에 캐시가 있으면 TourAPI를 재호출하지 않는다.

## 코드 스타일
- 모든 함수에 한글 docstring 1줄. 고등학생이 읽는다.
- 변수·함수명은 영어, 주석은 한글.
- 파일당 200줄 초과 금지. 넘으면 모듈 분리를 먼저 제안한다.
- 전역 상태 금지. 순수함수 우선.

## 작업 절차 (반드시 지킬 것)
1. 코드를 수정하면 즉시 `python -m pytest tests/ -q` 를 실행한다.
2. 되던 테스트가 깨지면 다음 작업으로 넘어가지 않는다.
3. 추천 로직을 바꾸면 `python -m src.recommender --smoke` 로 결과를 표로 출력해 눈으로 확인한다.
4. 추측하지 않는다. TourAPI 응답 스펙이 불확실하면 실제로 1건 호출해 확인한다.

## 하지 말 것
- 테스트를 통과시키려고 테스트 파일을 수정하는 것 (구현을 고쳐라)
- 실패를 try/except 로 덮어 숨기는 것
- 요청하지 않은 파일을 새로 만드는 것
- 인수 기준을 임의로 완화하는 것

## 인수 기준
@tests/ACCEPTANCE.md

## 현재 단계
Phase 1 = 완성본 개발. 교안화(lesson/)는 Phase 1 이 전부 PASS 된 뒤에 시작한다.
